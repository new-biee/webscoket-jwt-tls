apply plugin: "war"

bootWar {
    mainClass = "com.miraway.selfservice.WebsocketApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
